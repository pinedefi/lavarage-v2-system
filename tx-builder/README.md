# Transaction Builder

A utility for building Solana transactions for the margin protocol.

## Overview

The transaction builder takes transaction parameters as input and returns a serialized Solana transaction in wire format that can be submitted to the network.

## Usage

The transaction builder can be used either as a compiled binary or directly as a script.

### As a Binary
